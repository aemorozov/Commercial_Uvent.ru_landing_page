+   1. Вставить видео
+   2. Заменить логику появления текста в хедере
+   3. Подверстать по новому макету
+   4. Ввести критический css - решено, прикручен отдельный js файл, который вызывается gulp'ом, так же работает watcher
    5. Добавить скрипты для листания блоков
    6. Переписать JS на TS - вопросы:
        6.1 phoneNumber - не собирается проект потому что не понимает requare в данном файле
        6.2 videoControls - требует, что бы переменные не могли стать нулём
        6.3 настроить tsconfig
        6.4 TS мне врёт о том что, outerText не работает с элементами)
    7. Какое слово подставить для слогана, которое сотрётся и запишется правильное
        - афигенный
+   8. Не работает htmlMinify в gulpfile??? - после выключения и включения минификации JS и TS всё заработало, но не срабатывает с первого 
        раза создания dist, только после пересохранения