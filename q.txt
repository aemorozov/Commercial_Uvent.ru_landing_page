+   1. Вставить видео
+   2. Заменить логику появления текста в хедере
+   3. Подверстать по новому макету
+   4. Ввести критический css - решено, формируем critical css руками, его подключаем в header, 
    остальные стили грузим в конце страницы с асинхронными шрифтами.
        4.1 не происходит обновление данных при изменении critical css
        4.2 .pipe(gcssmq()) не работает
    5. Добавить скрипты для листания блоков
    6. Переписать JS на TS - вопросы:
        6.1 после компиляции VSCode в TS файлах пишет, что переменные уже объявлены и подчёркивает их красным 
        Duplicate function implementation.ts(2393)
        6.2 не происходит обновление файла при изменении
        6.3 мы переписываем всё на TS и так же компилируем в один app.js?
    7. Какое слово подставить для слогана, которое сотрётся и запишется правильное