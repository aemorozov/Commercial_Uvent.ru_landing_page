const uslugi: HTMLElement | null = document.querySelector('.for-slider')
const buttonLeft: HTMLElement | null = document.querySelector('.arrow-left')
const buttonRight: HTMLElement | null = document.querySelector('.arrow-right')
const shift = 500

const reviews: HTMLElement | null = document.querySelector('.reviews-for-flex')

// let scrollLeftVar = uslugi?.scrollLeft
// let isAnimationEnd = true

// uslugi?.addEventListener('scroll', scrollingX)
// reviews?.addEventListener('scroll', scrollingX)
buttons()

// function scrollingX() {
//     scrollLeftVar = uslugi?.scrollLeft
//     return scrollLeftVar
// }

function buttons() {
    if (!uslugi) {
        return
    }

    // uslugi?.addEventListener('animationend', () => {
    //     isAnimationEnd = true
    // })
    
    buttonRight?.addEventListener('click', () => {
        uslugi.scrollLeft += shift 
    })

    buttonLeft?.addEventListener('click', () => {
        uslugi.scrollLeft -= shift 
    })
}
